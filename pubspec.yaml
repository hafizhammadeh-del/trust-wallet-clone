name: trust_wallet_clone

description: A new Flutter project.

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Flutter plugins
  http: ^0.14.0
  provider: ^5.0.0
  shared_preferences: ^2.0.0
  dio: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.0
  build_web_compilers: ^3.2.0

flutter:
  uses-material-design: true

